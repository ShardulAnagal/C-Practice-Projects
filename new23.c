#include <stdio.h>
// This program adds 18% GST to a price of a product
void gst(float gst);

float main()
{
    gst=100;
    void gst(float gst);
    return 0;
}
void gst(float gst) {
    int p;
// {
//     int p;
//     printf("Enter the price of a product\n");
//     scanf("%f", &p);

    gst = (p * 0.18) + p;
    printf("Your final price of the product is %f\n", gst);
    
}